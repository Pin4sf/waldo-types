{
  "name": "@waldo/types",
  "version": "1.0.0",
  "description": "Shared TypeScript contracts for Waldo — health, agent, API, memory, adapter interfaces",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "watch": "tsc --watch --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "zod": "^3.23.0"
  }
}
