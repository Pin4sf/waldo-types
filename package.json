{
  "name": "@waldo/types",
  "version": "0.0.1",
  "description": "Shared TypeScript contracts for Waldo — cross-repo shapes only (health, agent, API responses)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "watch": "tsc --watch --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "zod": "^3.23.0"
  }
}
